services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: openblox
      POSTGRES_PASSWORD: openblox
      POSTGRES_DB: openblox
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:latest
    ports:
      - "6379:6379"

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: openblox
      MINIO_ROOT_PASSWORD: openblox
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data

volumes:
  pgdata:
  miniodata:
